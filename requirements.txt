spaces

aiohttp
numpy
scipy

# depth_anything_3 imports these but does not declare them (checked against
# its pyproject: addict in model/da3.py and utils/registry.py, tqdm in
# model/utils/gs_renderer.py -- both on the import path of its own api).
addict
tqdm
opencv-python
pillow
huggingface_hub
pyyaml

streetlevel @ git+https://github.com/YellowO2/streetlevel.git
torchvision
pillow-heif
transformers
# Unpinned -- bump this comment whenever panoramic-da3's HEAD moves and
# needs to be picked up, since a text-identical requirements.txt won't
# trigger HF's build cache to re-fetch it on its own. (bump: 2026-09-27-2)
panoramic-da3 @ git+https://github.com/YellowO2/panoramic-da3.git
