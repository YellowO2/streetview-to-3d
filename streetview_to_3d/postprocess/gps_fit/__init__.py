"""Putting DA3's output into the real world.

    load_pieces  fit each piece (a connected component of the scene's
                 edges) to its own GPS, at one scale for the whole scene
    fit          the similarity fit it uses, and the fixed origin that makes
                 every piece land in the same frame
"""
